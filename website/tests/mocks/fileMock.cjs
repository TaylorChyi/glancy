/**
 * 背景：
 *  - Jest 在解析静态资源（例如 SVG、PNG）时会尝试读取实际文件，引发 Node 环境不支持的问题。
 * 目的：
 *  - 将所有非原始导入的文件映射为固定字符串，确保测试运行时仅验证引用存在与否。
 * 关键决策与取舍：
 *  - 统一返回语义化占位符，避免在断言中出现绝对路径或二进制内容。
 * 影响范围：
 *  - 通过 `moduleNameMapper` 加载的所有资源型导入。
 * 演进与TODO：
 *  - 若未来需要基于文件名生成差异化桩，可替换为函数导出维持兼容。
 */
module.exports = "file-mock";
