/**
 * 背景：
 *  - 组件测试中常通过 `?raw` 直接获取 SVG 文本；真实加载依赖 Vite 插件，Jest 中并不存在。
 * 目的：
 *  - 提供轻量的 SVG 片段占位，维持依赖该字符串的渲染与快照测试稳定。
 * 关键决策与取舍：
 *  - 选用最小矩形结构确保可预期渲染结果，同时保持 `currentColor` 便于主题断言。
 * 影响范围：
 *  - 所有以 `?raw` 方式导入 SVG 的单测与快照。
 * 演进与TODO：
 *  - 若后续需要比对 SVG 结构，可将字符串升级为可配置工厂函数。
 */
module.exports = "<svg><rect fill='currentColor'/></svg>";
