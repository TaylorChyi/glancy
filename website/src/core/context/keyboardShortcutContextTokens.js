/**
 * 背景：
 *  - 快捷键上下文需要向外暴露统一的动作标识以便组件判断当前状态。
 * 目的：
 *  - 提供集中管理的快捷键动作常量，同时遵守 React Fast Refresh 对组件文件的限制。
 * 关键决策与取舍：
 *  - 将常量抽离到独立模块，避免在提供者文件中导出非组件实体触发热更新告警；如未来常量增多可演进为枚举或配置读取。
 * 影响范围：
 *  - 依赖快捷键上下文的组件、hooks 及测试将共享同一常量定义，保证行为一致。
 * 演进与TODO：
 *  - TODO: 接入服务端配置或多租户策略时，可在此模块扩展映射与校验逻辑。
 */
export const KEYBOARD_SHORTCUT_RESET_ACTION = "__GLOBAL_RESET__";
