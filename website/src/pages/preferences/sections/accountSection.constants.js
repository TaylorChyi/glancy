/**
 * 背景：
 *  - AccountSection 原先直接导出常量与组件，触发 react-refresh 在开发态的导出约束。
 * 目的：
 *  - 以集中常量文件承载跨分区共享的字段类型定义，让组件文件专注于渲染职责。
 * 关键决策与取舍：
 *  - 采用简单常量模块而非上下文：常量无运行态依赖，无需引入额外抽象或懒加载开销。
 * 影响范围：
 *  - 偏好设置页面下引用账号用户名字段类型的逻辑。
 * 演进与TODO：
 *  - 后续若字段类型扩展，可在此模块集中维护并暴露枚举式接口。
 */
export const ACCOUNT_USERNAME_FIELD_TYPE = "username-editor";
