/**
 * 背景：
 *  - 语言菜单抽离为共享 UI 组件后，保留原入口以平滑迁移既有引用与测试。
 * 目的：
 *  - 通过再导出方式维护向后兼容，避免一次性调整所有导入路径。
 * 关键决策与取舍：
 *  - 维持最薄包装，不增加额外逻辑；
 *  - 当 ChatInput 内部完成重构，可考虑直接改用新路径并删除本桥接层。
 */
export { default } from "@/components/ui/LanguageMenu/index.jsx";
