# FR-014 频率与考试标签（Will not，MVP）

- **前置**
  - 任意登录用户；浏览器 DevTools Network 面板开启以捕捉 `/frequency` 或 `/exam` 请求。
  - 进入 UC-01 结果页，保持默认模块布局。
- **步骤**
  1. 查询 `proactive`，滚动至“频率与考试”预留模块位置。
  2. 监听查询期间的网络请求，确认未访问频率相关 API。
  3. 将 UI 切换为暗色主题，刷新页面，再次检查占位状态。
- **期望**
  - 模块位置显示灰态占位文案“暂不提供频率与考试标签”，无交互控件。
  - 网络层没有触发频率/考试数据请求，渲染不影响其他模块布局。
  - 主题切换后占位样式依然正确，说明该功能在 MVP 关闭且可追溯。
- **数据**
  - 词条：`proactive`。
  - 需要捕捉的请求：`/api/v1/frequency*`（应不存在）。
  - 截图：UI 占位态，用于发布评审佐证。
- **优先级**：P3（Will not）
- **关联 UC**：UC-01
- **关联 AC**：AC-FR-014，AC-UC-01
